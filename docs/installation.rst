.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install beholder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv beholder
    $ pip install beholder
